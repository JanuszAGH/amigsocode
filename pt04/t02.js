class Algebra {
    static sum(a, b) {
        return a + b;
    }

    static difference(a, b) {
        return a - b;
    }

    static product(a, b) {
        return a * b;
    }

    static quotient(a, b) {
        return a / b;
    }
}

let sum = Algebra.sum(2, 3);
console.log(sum);